<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Mi perfil</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<body class="container py-5">

  <h1 class="mb-4">👤 Mi Perfil</h1>

  {{#if user}}
    <ul class="list-group">
      <li class="list-group-item"><strong>Nombre:</strong> {{user.first_name}} {{user.last_name}}</li>
      <li class="list-group-item"><strong>Email:</strong> {{user.email}}</li>
      <li class="list-group-item"><strong>Edad:</strong> {{user.age}}</li>
      <li class="list-group-item"><strong>Rol:</strong> {{user.role}}</li>
    </ul>

    <form action="/api/auth/signout" method="post" class="mt-4">
      <button class="btn btn-danger">Cerrar sesión</button>
    </form>
  {{else}}
    <p class="alert alert-warning">No hay datos de usuario</p>
    <a href="/login" class="btn btn-primary">Vuelvo pronto</a>
  {{/if}}

</body>
</html>